interface:
  display_name: "Visio Copy"
  short_description: "Recreate raster and stacked-grid diagrams as editable Visio vector drawings."
  default_prompt: "Use visio_copy to trace a reference image into editable Visio shapes with correct layers, pixel coordinates, stacked-grid cell matrices, arrows, text sizing, and preview validation."
